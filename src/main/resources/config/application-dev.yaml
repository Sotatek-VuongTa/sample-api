spring:
  datasource:
    url: jdbc:h2:mem:musinsa-db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: musinadb
    password: musinapw

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect

  flyway:
    enabled: false
    baseline-on-migrate: true
    locations: classpath:db/migration
  jwt:
    secret: musina-secret-key-123